# - Create a variable named `numbers`
#   with the following content: `[4, 5, 6, 7]`
# - Print the third element of `numbers`

numbers = [4,5,6,7]
print(numbers[2]) # the count in lists starts from 0 not 1, element [2] is the third element
